meta {
  name: 03 - show task
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/projects/{{projectId}}/tasks/{{taskId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

headers {
  Accept: application/json
}

docs {
  # Show one task. 404 on cross-owner / missing.

  Expected: 200, `data.task` shape per TaskResource.
}
